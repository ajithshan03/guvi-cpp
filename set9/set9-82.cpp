#include<iostream>
using namespace std;
int main()
{
    int a,b;
    cout<<"Enter the two numbers A and B";
    cin>>a>>b;
    int area = a*b;
    cout<<"Area of the farm is :"<<area;

    return 0;
}
